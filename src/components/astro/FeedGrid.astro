---
// Product of Launch Maniac llc, Las Vegas, Nevada - (725) 444-8200 support@launchmaniac.com
// Grid layout wrapper for feed panels

interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class:list={['grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 2xl:grid-cols-4 gap-4', className]}>
  <slot />
</div>
